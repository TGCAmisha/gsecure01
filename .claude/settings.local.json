{
  "permissions": {
    "allow": [
      "Bash(command -v gh)",
      "Bash(gh --version)",
      "PowerShell(git checkout -b claude/redesign-brand-polish)",
      "PowerShell(git add -A)"
    ]
  }
}
